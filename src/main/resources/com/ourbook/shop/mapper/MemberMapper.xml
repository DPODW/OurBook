<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ourbook.shop.mapper.MemberMapper">

    <insert id="sellerInsert">
        insert into seller_tb(name,id,pwd,email,role)values(#{commonName},#{commonId},#{commonPwd},#{commonEmail},#{commonRole});
    </insert>

    <insert id="buyerInsert">
        insert into buyer_tb(name,id,pwd,email,role)values(#{commonName},#{commonId},#{commonPwd},#{commonEmail},#{commonRole});
    </insert>

    <insert id="naverInsert">
        insert into buyer_tb(name,id,pwd,email,role)values(#{name},#{id},#{pwd},#{email},#{role});
    </insert>

</mapper>