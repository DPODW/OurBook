<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ourbook.shop.mapper.TestMapper">

    <insert id="save">
        insert into ourbook_member(name,id,pwd,email,role)values(#{name},#{id},#{pwd},#{email},#{role});
    </insert>

</mapper>